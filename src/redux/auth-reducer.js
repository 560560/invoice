let initialState = {
    isAuth: false,
    username: null,
    userAvatarSrc: null,
    errorMessage: null,
}

const authReducer = (state= initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}



export default authReducer